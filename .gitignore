# Secrets — never commit
.env
.env.*
*.key

# OS / editor / local tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# Local server logs
*.log

# Blender working files (keep scripts + final assets, not heavy intermediates)
blender/renders/
*.blend
*.blend1
